#include "library.h"

int main()
{
 //    //Testing the hashTable
	// Table keywordsTable=create(53);
	// int i=0;
	// for(i=0;i<24;i++)//24 is keyword table size
 //        insert(keywordsTable,keywords,i);
 //    link c=lookup(keywordsTable,"while",keywords);
 //    if(c)
 //    printf("%s",keyword_tokens[c->index]);
	return 0;
}
